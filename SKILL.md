---
name: boss-feishu-greeter-mac
description: Mac 版 BOSS 直聘飞书规则主动打招呼 skill。复用用户已登录的有头 Chrome，通过 AppleScript 接管页面，在用户手动切好岗位和筛选后，按飞书任务配置、阈值和赋分标准执行候选人评分、低频自动打招呼和飞书回写。
---

# BOSS 飞书规则打招呼 Mac 版

## 能力边界

这个 skill 只做主动打招呼：

- 复用当前已登录的有头 Chrome
- 读取飞书多维表格里的岗位规则
- 校验 BOSS 当前岗位与用户指定任务一致
- 扫描当前职位候选人
- 按飞书规则硬筛和评分
- 对符合标准的人自动点击打招呼
- 写入本地 run state 并回写飞书任务记录和打招呼记录

它不处理消息、不处理简历、不上传简历。默认不自动切岗位、不自动打开筛选。

## RBT 风格稳定性协议

执行时必须遵守：

1. 只允许复用当前有头 Chrome，禁止无头、CDP、独立 Profile。
2. 同一轮只允许一个 BOSS 页签；检测到多个 BOSS 页签则停止。
3. 默认要求用户已手动切好岗位和筛选；当前岗位城市必须与任务城市一致，否则停止。
4. 页面必须处于 BOSS 企业端推荐页；若跳到首页、登录页、验证码、风控页，立即停止。
5. 飞书任务必须由 `城市 + 岗位类型` 唯一匹配并启用；自动点击默认允许。
6. 页面筛选由用户手动完成；脚本只读取筛选后的候选人并硬筛评分。
7. 同一候选人同一轮最多点击一次。
8. 点击后必须校验按钮状态是否变成 `继续沟通` 或页面是否明确进入已沟通状态。
9. 连续 10 轮没有新的合格候选人时，点击顶部蓝绿色 `推荐` 标签刷新推荐池；最多刷新 3 次，第三次刷新后仍连续 10 轮无合格候选人才提前结束。
10. 出现账号异常、封禁、上限、验证码、滑块、登录失效、频率提示，立即停止；停止时必须输出并保存页面上的完整风控警告文本，不要只输出命中的关键词。
11. 用户执行 `stop` 后，一个动作周期内停止并输出快照。

## 命令

```bash
bash scripts/check_chrome.sh
node scripts/greeter_260505.mjs feishu-test
node scripts/greeter_260505.mjs inspect
node scripts/greeter_260505.mjs dry-run <城市+岗位类型>
node scripts/greeter_260505.mjs run-task <城市+岗位类型> [本次上限]
node scripts/greeter_260505.mjs auto-greet <城市+岗位类型>
bash scripts/stop.sh
```

`dry-run` 只筛选评分，不点击。

`run-task` 是默认执行入口：先自动 dry-run 做无点击校验；只要 Chrome、BOSS 页面、飞书规则、岗位城市和风控检查都通过，就必须直接进入 `auto-greet`，不要向用户询问是否继续。dry-run 当前可见候选人命中数为 0 时，也继续进入 `auto-greet`，由滚动和推荐池刷新逻辑寻找后续候选人。`run-task` 会复用 dry-run 的规则和页面校验结果，不重复读取和等待。

`auto-greet` 会自动点击，但必须同时满足：

- 飞书字段 `招呼量` 大于 0
- 当前 BOSS 页岗位城市与飞书 `城市` 匹配
- 没有任何风控信号

## 飞书规则来源

岗位规则字段见 `references/feishu_schema.md`。

执行前只读取三张规则表：`任务配置表`、`赋分标准`、`阈值配置`。`任务记录` 和 `打招呼记录` 不在执行前读取，只在任务结束后直接回写。

核心字段：

- 城市
- 岗位类型
- 是否启用
- 招呼量
- 今日已完成、今日剩余、任务状态只在 `最后更新时间` 是当天时沿用；如果最后更新时间不是今天，则执行前按新一天处理，今日已完成视为 0、今日剩余视为招呼量
- 筛选标准-最低年龄
- 筛选标准-最高年龄
- 筛选标准-活跃度
- 筛选标准-是否与同事交换简历
- 细分区域
- 阈值配置：岗位类型、城市、通过阈值、规则版本、规则说明；每次执行都会重新读取
- 赋分标准：硬过滤、加分、减分规则；`正向词` 是命中词，`排除词` 是该条规则的排除词；若填写 `在职时长大于等于(月)`，正向词必须和满足时长的经历段同时命中才计分
- 点击节奏由代码控制为相邻两次点击间隔 3-5 秒随机；寻找、滚动、评分耗时会抵扣等待时间
- 一批合格候选人会在同一个 AppleScript 会话内按节奏点击并轮询校验，减少反复接管 Chrome 的耗时
- 无合格候选人时滚动后自适应等待页面变化；页面候选人变化后立即继续扫描，不固定空等

## 默认执行顺序

1. preflight：Chrome/BOSS/飞书/停止信号检查
2. load-task：只读取 `任务配置表`、`赋分标准`、`阈值配置`
3. page-guard：确认当前推荐页与城市匹配
4. dry-run-check：无点击扫描当前可见候选人并保存校验快照；校验通过后不暂停、不二次确认，并在 `run-task` 内复用预检结果
5. use-current-filter：沿用用户已设置好的页面筛选
6. scan：读取当前可见候选人
7. score：硬筛、评分、排序
8. greet：低频批量点击 Top N，并在同一个接管会话内轮询校验
9. scroll：无合格人选时滚动并等待候选人变化，减少固定等待
10. refresh-pool：连续 10 轮无合格候选人时点击 `推荐` 刷新推荐池，最多 3 次
11. record：回写任务配置、任务记录、打招呼记录；不预读记录表
12. summary：输出结果，包含本地耗时统计

## 自动化默认策略

用户要求“开始、继续、跑满、执行某城市任务”时，默认使用 `run-task`，不要把单独 `dry-run` 作为最终步骤，也不要在 dry-run 之后询问是否继续。

只有以下情况需要停下并提醒用户：

- 当前 BOSS 页岗位城市与任务城市不一致
- BOSS 页签数量不唯一
- 未登录、验证码、风控、频率限制、账号异常或页面不在推荐页；如果是风控类停止，必须在输出和本地状态中包含完整 `riskWarning`
- 飞书没有匹配且启用的任务，或 `招呼量` / 剩余量为 0
- 脚本抛出点击校验失败、回写失败或明确风险信号
